Template.updateTrainingForm.helpers({
  selectedLocation: function() {
    /*
    location = Locations.findOne(this.location);
    console.log(location);
    return {label: location.name, value: location._id};
    */
  }
});
